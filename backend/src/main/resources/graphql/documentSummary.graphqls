extend type Query {
  documentSummaryByFileId(fileId: ID!): DocumentSummary
}

type DocumentSummary {
  summaryId: ID!
  fileId: ID!
  status: SummaryStatus!
  summaryText: String
  modelVersion: String
}

enum SummaryStatus {
  REQUESTED
  COMPLETED
  FAILED
}

