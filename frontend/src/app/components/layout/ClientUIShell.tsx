"use client";

export default function ClientUIShell({
                                          children,
                                      }: {
    children: React.ReactNode;
}) {
    return <>{children}</>;
}
